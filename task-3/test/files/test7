class Test {
    public static void bubbleSort(int[] arr) {
        int n = arr.length();
        for (int i = 0;
         i < n - 1;
          i+=1)

         {
            for (int j = 0; j < n  - i - 1;


             j+=1)

            {
  if (arr[j] > arr[j + 1]) {
                    int temp = arr[j];
        arr[j] = arr[j + 1];
                                                     arr[j +  1] = temp;
                }
                }
                }
        while (true) { c -= 1;}

        if (false) {
            someValue /= 0;
        } else {
            return null;
        }
        return null;
    }
}